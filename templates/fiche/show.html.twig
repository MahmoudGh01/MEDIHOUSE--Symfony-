

{% block title %}Fiche{% endblock %}

{% block content %}
    <h1>Fiche</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fiche.id }}</td>
            </tr>
            <tr>
                <th>Id_Patient</th>
                <td>{{ fiche.IdPatient }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ fiche.age }}</td>
            </tr>
            <tr>
                <th>RendezVous</th>
                <td>{{ fiche.RendezVous ? fiche.RendezVous|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fiche_index') }}">back to list</a>

    <a href="{{ path('app_fiche_edit', {'id': fiche.id}) }}">edit</a>

    {{ include('fiche/_delete_form.html.twig') }}
{% endblock %}
